<UserControl x:Class="GestioneCopie.WPF.App.Content.StartContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ScrollViewer>
            <StackPanel x:Name="Panel" MinWidth="150">
                <TextBlock Text="{Binding Author}" Style="{StaticResource Heading2}" Margin="0,0,0,8" />
                <TextBlock Text="{Binding Classe}" Style="{StaticResource Heading2}" Margin="0,0,0,8" />
            </StackPanel>
        </ScrollViewer>

        <Image Source="pack://application:,,,/GestioneCopie.WPF.App;component/Images/leonardo1.jpg"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Width="150"
                       Height="170"/>
    </Grid>
</UserControl>
